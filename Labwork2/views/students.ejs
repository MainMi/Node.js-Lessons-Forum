<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Information about the students</title>
    <link rel="stylesheet" href="/css/students.css">
</head>
<body>

<h1>Information about the students</h1>

<ul>
    <% Object.entries(studentsInfo).forEach(([student, studentInfo]) => { %>
        <li>
            <strong>Name:</strong> <a href="/student/<%= student %>"><%= studentInfo.fullname %></a><br>
            <strong>Faculty:</strong> <%= studentInfo.faculty %><br>
            <strong>Course:</strong> <%= studentInfo.course %><br>
            <strong>Group:</strong> <%= studentInfo.group %><br>
            <strong>Email:</strong> <%= studentInfo.email %><br>
            <strong>Phone:</strong> <%= studentInfo.phone %>
        </li>
    <% }); %>
</ul>

</body>
</html>
